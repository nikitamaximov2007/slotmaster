Local casino logo assets.

Use one file per casino slug. The current UI reads paths from src/data/casinos.ts
through the logoImage field and renders them with next/image.

Preferred format: SVG.
Fallback formats: PNG or WebP.
